该文件为 AI Coding 在此代码库中工作时提供指导。

<!-- @Author:Jewei -->
请始终使用中文回复。

### 代码规范
- 遵循 SOLID、DRY、KISS、YAGNI；
- 代码保持模块化、结构化，结构清晰，可读性高，易维护
- 一个文件代码量不能超过1000 行，一个函数方法不能超过80 行；超过需要按职责拆分。
- 代码必须在写文件注释和方法注释以及合适的关键逻辑写注释


# 用于减少常见 LLM 编程错误的行为准则。根据需要与项目特定的开发规范合并使用。

**权衡：** 这些准则倾向于“谨慎优先”而不是“速度优先”。对于简单任务，请自行判断是否需要严格遵循。

---

## 1. 编码前先思考

**不要自作假设。不要隐藏疑惑。主动说明权衡。**

在实现之前：

- 明确说明你的假设。如果不确定，请提出问题。
- 如果存在多种理解方式，请列出来 —— 不要默默选择其中一种。
- 如果存在更简单的方案，请指出。必要时提出反对意见。
- 如果某些内容不清楚，停止执行。说明困惑点并询问。

---

## 2. 优先考虑简单方案

**用最少的代码解决问题。不要提前设计不存在的需求。**

- 不实现需求之外的功能。
- 不为一次性代码设计抽象层。
- 不添加未被要求的“灵活性”或“可配置性”。
- 不为不可能发生的场景添加错误处理。
- 如果你写了 200 行代码，而实际只需要 50 行，重新简化。

问自己：

> “一个资深工程师会认为这个实现过度复杂吗？”

如果答案是“会”，就进行简化。

---

## 3. 精准修改

**只修改必须修改的内容。只清理你自己造成的问题。**

修改已有代码时：

- 不要“顺便优化”附近的代码、注释或格式。
- 不要重构没有问题的代码。
- 保持现有代码风格，即使你有不同的写法偏好。
- 如果发现无关的废弃代码，指出它 —— 不要删除。

当你的修改产生孤立代码时：

- 删除由于**你的修改**导致不再使用的 import、变量、函数。
- 不要删除原本存在的无用代码，除非用户要求。

判断标准：

> 每一行修改都应该能够直接追溯到用户的需求。

---

## 4. 目标驱动执行

**定义成功标准。持续循环直到验证通过。**

将任务转换为可验证的目标：

- “添加校验” → “为无效输入编写测试，然后让测试通过”
- “修复 Bug” → “先编写能够复现问题的测试，然后修复使其通过”
- “重构 X” → “确保重构前后测试均通过”

对于多步骤任务，先给出简短计划：

```text
1. [步骤] → 验证方式：[检查项]
2. [步骤] → 验证方式：[检查项]
3. [步骤] → 验证方式：[检查项]
```

明确的成功标准可以让你独立完成循环验证。模糊的标准（例如：“让它能工作”）会导致频繁需要用户澄清。

---

**判断这些准则是否有效，这些准则正在发挥作用时:** Diff 中出现更少的不必要修改； 因过度设计导致的返工更少； 在实现之前主动提出澄清问题，而不是犯错后再返工。


<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan
at specs/004-cli-command-redesign/plan.md
<!-- SPECKIT END -->
