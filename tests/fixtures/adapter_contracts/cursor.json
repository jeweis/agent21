{
  "schema_version": "1.0",
  "agent": "cursor",
  "status": "implemented",
  "capabilities": {
    "instructions": {
      "mode": "native",
      "notes": "默认直接读取权威指令。"
    },
    "skills": {
      "mode": "native",
      "notes": "默认直接读取权威 Skills。"
    },
    "mcp": {
      "mode": "transform",
      "notes": "MCP 需要工具专用输出。"
    }
  },
  "source_inputs": [
    "AGENTS.md",
    ".agents/skills"
  ],
  "managed_outputs": [
    {"path": ".cursor/mcp.json", "kind": "file", "managed": true, "snapshot": true}
  ],
  "platform_modes": {
    "linux": ["auto", "copy", "symlink"],
    "macos": ["auto", "copy", "symlink"],
    "windows": ["copy"]
  },
  "contract_cases": [
    "cursor-instructions",
    "cursor-skills",
    "cursor-mcp"
  ]
}
